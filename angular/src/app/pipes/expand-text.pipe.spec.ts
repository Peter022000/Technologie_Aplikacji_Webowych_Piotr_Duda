import { ExpandTextPipe } from './expand-text.pipe';

describe('ExpandTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpandTextPipe();
    expect(pipe).toBeTruthy();
  });
});
