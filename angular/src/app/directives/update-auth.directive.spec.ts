import { UpdateAuthDirective } from './update-auth.directive';

describe('UpdateAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateAuthDirective();
    expect(directive).toBeTruthy();
  });
});
